# HawkinsOps MITRE ATT&CK Mapper Configuration

# Confidence threshold for technique mapping (0.0 - 1.0)
confidence_threshold: 0.5

# Maximum techniques to map per event
max_matches_per_event: 5

# Enable verbose logging
verbose: false
