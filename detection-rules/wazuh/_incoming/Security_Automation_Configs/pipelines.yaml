pipelines:
  default_lab:
    description: "Full lab documentation pipeline"
    stages:
      - architect
      - sensor
      - executor
      - chronicler
    output_format: "lab_writeup"

  detection_rule:
    description: "Detection rule documentation"
    stages:
      - architect
      - sensor
      - wildcard
      - executor
    output_format: "detection_doc"

  quick_note:
    description: "Fast processing for simple notes"
    stages:
      - architect
      - executor
    output_format: "quick_doc"

  linkedin_post:
    description: "Convert technical work to LinkedIn content"
    stages:
      - architect
      - executor
      - chronicler
    output_format: "linkedin"

  portfolio_case_study:
    description: "Full case study for portfolio"
    stages:
      - architect
      - sensor
      - wildcard
      - executor
      - chronicler
    output_format: "case_study"

  weekly_summary:
    description: "Progress summary generation"
    stages:
      - sensor
      - chronicler
    output_format: "summary"